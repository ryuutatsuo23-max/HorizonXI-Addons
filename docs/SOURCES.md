# Sources and evidence boundaries

Profile snapshot: `2026-09-04-foundation.17`.

## Upstream inspiration

- [HiPotionQ8/XIchecklist](https://github.com/HiPotionQ8/XIchecklist): behavioral inspiration and requested conversion target. No Lua implementation code was copied. Its `maps/story.lua` table at commit [`04baf17b2b0373883407a94ce0b6a72274a31ba6`](https://github.com/HiPotionQ8/XIchecklist/blob/04baf17b2b0373883407a94ce0b6a72274a31ba6/maps/story.lua) supplies the seven client-log orderings: Bastok indices 0 through 92, 82 named San d'Oria entries through index 119, 90 named Windurst entries through index 96, 146 named Jeuno entries through index 186, 91 named Other entries through index 209, 57 named Outlands entries through index 203, and 72 named Aht Urhgan entries through index 103. Commented-out placeholders are not imported, and the upstream leading `+` annotation is removed from displayed Jeuno/Other/Outlands titles without changing indices.
- [Windower/Lua packet definitions](https://github.com/Windower/Lua/tree/dev/addons/libs/packets) and Windower's generated key-item resources: protocol and numeric-ID cross-checks for the read-only map implementation. No Windower implementation code is bundled.

## Ashita v4 behavior

The live checks use Ashita v4's installed interface annotations and established addon patterns:

- resolve imported spells with `IResourceManager:GetSpellById`, validate the English name and magic-skill ID, then read `IPlayer:HasSpell`; name lookup remains a fallback for entries without an explicit ID;
- resolve and validate key-item IDs with `IResourceManager:GetString`, then read the incoming `0x055` ownership bit; a positive `IPlayer:HasKeyItem` remains a pre-log fallback;
- persist per-character preferences with Ashita's `settings` library;
- read current numeric magic-related skills with `IPlayer:GetCombatSkill`, `combatskill_t:GetSkill`, and `combatskill_t:IsCapped`;
- read spell job requirements from the already name- and skill-validated `ISpell.LevelRequired` client resource, limited to Horizon's twenty level-75-era jobs and levels 1 through 75;
- draw the checklist with Ashita's `imgui` library.

Version 0.17.0 passively reads the incoming `0x055` key-item log for map ownership and the incoming `0x056` quest log for the seven quest-area catalogs. The key-item layout has 64 availability bytes from offset `0x04`, 64 examined bytes, and a group value at offset `0x84`; HXIChecklist reads only the availability bytes and group. The first six quest areas use 32 flag bytes from offset `0x04` and a type value at offset `0x24`; San d'Oria current/completed types are `0x0050`/`0x0090`, Bastok types are `0x0058`/`0x0098`, Windurst types are `0x0060`/`0x00A0`, Jeuno types are `0x0068`/`0x00A8`, Other types are `0x0070`/`0x00B0`, and Outlands types are `0x0078`/`0x00B8`. Aht Urhgan uses `0x0080`/`0x00C0` with only 16 quest-flag bytes from `0x04`; bytes from `0x14` contain mission/Assault data and are ignored. The Jeuno, Other, and Outlands pairs are corroborated by the pinned XIchecklist [`util/quests.lua`](https://github.com/HiPotionQ8/XIchecklist/blob/04baf17b2b0373883407a94ce0b6a72274a31ba6/util/quests.lua) mapping. It never changes, blocks, injects, or requests a packet.

Decoded key-item groups and each nation's paired quest logs are hex-encoded separately into a versioned cache within Ashita's settings library. Ashita v4 stores that settings block under its character-name and server-ID path and invokes the registered callback when the active character changes. Invalid, incomplete, or absent cache data fails closed to `UNKNOWN`; packet data from one character is never intentionally reused for another.

## HorizonXI magic catalog

The 2026-09-03 snapshot follows the six user-requested HorizonXI Wiki category pages:

- [Dark Magic](https://horizonffxi.wiki/Dark_Magic): 15 spells.
- [Divine Magic](https://horizonffxi.wiki/Divine_Magic): 8 spells.
- [Elemental Magic](https://horizonffxi.wiki/Elemental_Magic): 60 spells.
- [Enfeebling Magic](https://horizonffxi.wiki/Enfeebling_Magic): 19 spells.
- [Enhancing Magic](https://horizonffxi.wiki/Enhancing_Magic): 76 spells.
- [Healing Magic](https://horizonffxi.wiki/Healing_Magic): 22 spells.

Those six catalogs contain 200 unique player spells. A row was included only when it appeared in the relevant HorizonXI Wiki category and matched a learnable client spell resource in that same skill with at least one job level at or below HorizonXI's level-75 cap. Client IDs, skill IDs, learnable flags, and job levels were cross-checked against [Windower/Resources](https://github.com/Windower/Resources) at commit `67948a3ce609ac614e889002268470859be319d5`.

Category and guide pages were not treated as spells. `Enlight` was excluded because its client job level is 85. The monster-only/unlearnable `Bindga`, `Diaga II`, and `Slowga` resources were excluded. `Sleepga` and `Sleepga II` use the learnable client IDs 273 and 274 rather than same-name unlearnable resources. These filters are deliberately conservative; a wiki listing is evidence, not a guarantee that a spell is obtainable on the current server build.

Version 0.5.0 adds 116 ownership rows from the current Horizon job lists:

- [Summoner summons](https://horizonffxi.wiki/Summoner#Summons): 17 rows, comprising the nine listed avatars and eight elemental spirits. Blood Pacts and unrelated pages in the broader Summoning Magic category are not ownership rows.
- [Ninja Ninjutsu list](https://horizonffxi.wiki/Ninja#Ninjutsu_List): 23 rows. Ninja tools, guides, and client spells absent from the Horizon job list are excluded.
- [Bard song list](https://horizonffxi.wiki/Bard#Song_List): 76 rows. `Raptor Mazurka` and `Adventurer's Dirge` exist in the client resources but are absent from the current Horizon Bard list and are therefore excluded rather than guessed available.

These additions bring `All Magic` to 316 unique ownership entries. Summoning uses client skill ID 38, Ninjutsu 39, and Songs 40. The displayed `Lightning Threnody` row validates against the client's shortened English resource name `Ltng. Threnody`. Singing, String Instrument, and Wind Instrument are numeric skill values—not additional song ownership lists—and appear only in the separate live `Skill Levels` view.

The job-level text beside each ownership row comes from the same locally installed client spell resource used to validate its ID, English name, and magic-skill ID. Only WAR through SCH and requirements from level 1 through 75 are displayed. This is client metadata for the private test installation, not a new claim that every listed spell is currently obtainable on HorizonXI.

## Numeric skill levels

The `Skill Levels` view reads client combat-skill indices 32 through 42: Divine, Healing, Enhancing, Enfeebling, Elemental, Dark, Summoning, Ninjutsu, Singing, String Instrument, and Wind Instrument. Each row shows the raw integer returned by Ashita plus Ashita's boolean capped flag. It does not calculate caps from jobs, levels, merits, equipment, food, or server rules.

This view is informational and live-only. It is not added to the ownership catalog or progress denominator and is not written to the character cache. If the character is not logged in or a memory read fails, the affected value remains visibly unavailable rather than being inferred.

## HorizonXI maps and nation quests

The Maps catalog contains the 72 rows in the HorizonXI Wiki's [Magical Maps table](https://horizonffxi.wiki/Category:Magical_Maps): 28 Original-area maps, 16 Rise of the Zilart maps, 17 Chains of Promathia maps, and 11 Treasures of Aht Urhgan maps. A `wiki_listed` label means only that the table identifies the map; it is not a claim of current server availability. Rows with an existing individual page link to it; rows whose table link is a red link retain the category table as their evidence source.

All 72 numeric IDs and English resource names were cross-checked against Windower/Resources `resources_data/key_items.lua` at commit [`67948a3ce609ac614e889002268470859be319d5`](https://github.com/Windower/Resources/blob/67948a3ce609ac614e889002268470859be319d5/resources_data/key_items.lua). Runtime code also verifies each numeric ID back against Ashita's English key-item resource name before reading the corresponding `0x055` ownership bit. The broader client resource contains maps absent from HorizonXI's table; those are excluded. In particular, `Map of the Uleguerand Range` and temporary Assault-area maps are not inferred into this catalog merely because client records exist.

Vendor costs come only from the Purchased tables in the HorizonXI Wiki [Map Guide](https://horizonffxi.wiki/Map_Guide). Thirty catalog maps have a listed price: 27 in gil and three in Imperial Standing. Price spelling is normalized to `gil`; amounts and currency are otherwise preserved. The guide's `Nahmau` and `Havlung` display typos are matched conservatively through their linked Nashmau and Halvung map targets.

The other 42 rows use the corresponding `Obtained` value from the HorizonXI Wiki [Magical Maps table](https://horizonffxi.wiki/Category:Magical_Maps). Quest titles are prefixed `Quest`, mission rows are prefixed `Mission`, and the source's generic `Mini-quest`, `Chest`, and `Coffer` wording is retained. `Map of Alza'daal Ruins` uses the Map Guide's more specific `Undersea Scouting` mission title in place of the category table's NPC-only value. These are sourced acquisition summaries, not claims that the current private-server implementation exactly matches every wiki step.

The Bastok catalog contains every named XIchecklist client slot from index 0 through 92. The original nineteen pilot rows preserve the stable `HXQ-0001` through `HXQ-0019` IDs and their local pilot notes; new rows use index-derived IDs. The [HorizonXI Bastok Quests category](https://horizonffxi.wiki/Category:Bastok_Quests) supplies current source links, starting-location groupings, and its displayed fame column. A wiki listing is evidence, not a guarantee that the quest is active or implemented identically on the current server.

The category page has location/fame table rows for 88 client quests: 67 show numeric Bastok fame levels and 21 show no listed fame value. Four more client quests appear only in the category's flat quest list, so their fame remains `Unknown`. `Synergistic Pursuits` appears in a location table despite being absent from that flat list; the location-table row is retained as its evidence. `A Proper Burial` appears in XIchecklist's client ordering but in neither HorizonXI category section, so both its evidence state and fame remain explicit `Unknown`. This produces 92 client rows with some category evidence and one unresolved availability row without inventing missing facts.

Evidence exceptions remain visible:

- `HXQ-0003` (`A Proper Burial`) is `unknown` and excluded from known totals.
- `HXQ-0012` (`All by Myself`) retains the pilot's `reported_inactive` boundary and is excluded from progress totals.
- `HXQ-0015` (`Father Figure`) retains the pilot's `reported_active` label; that report still requires private-server verification.
- `A Discerning Eye (Bastok)`, `Bait and Switch`, `Fully Mental Alchemist`, and `Trust: Bastok` are category-listed but have `Unknown` fame because no matching location/fame row exists.

There is no manual completion fallback for the mapped Bastok catalog. Until both live logs or a valid cache exist for the current character, rows remain `UNKNOWN`. A private-server result is still not proof of HorizonXI approval or universal server compatibility.

The San d'Oria catalog contains all 82 named entries in XIchecklist's San d'Oria client-log ordering. The [HorizonXI San d'Oria Quests category](https://horizonffxi.wiki/Category:San_d%27Oria_Quests) provides location, type, NPC, and fame evidence for 79 entries: 60 numeric fame values and 19 values not listed. [Port San d'Oria](https://horizonffxi.wiki/Port_San_d%27Oria) separately references `Lure of the Wildcat (San d'Oria)`, but its individual page and fame value are unresolved. `Atelloune's Lament` and `Trust: San d'Oria` have no resolved current Horizon source, so their availability and fame stay `Unknown`. This yields 80 rows with some Horizon evidence, two explicit availability unknowns, and three unknown fame values without inventing missing facts.

San d'Oria state uses its own character-cache key and does not change the established Bastok cache format. There is no manual completion fallback for any nation catalog. Until both live logs or a valid cache exist for the relevant nation and character, its rows remain `UNKNOWN`.

The Windurst catalog contains all 90 named entries in XIchecklist's Windurst client-log ordering. The [HorizonXI Windurst Quests category](https://horizonffxi.wiki/Category:Windurst_Quests) matches 87 client rows and supplies 65 numeric fame values plus 22 values not listed. Four mapped rows—`Let Sleeping Dogs Lie`, `Nothing Matters`, `Escort for Hire (Windurst)`, and `A Discerning Eye (Windurst)`—are explicitly marked unavailable by that table and remain visible as `UNAVAILABLE` when that filter is enabled. Its two additional rows, `A Chocobo Riding Game (Windurst)` and `Dyer's Woad Quest`, have no XIchecklist client-log mapping and are not silently inserted. Current pages for [Tree Saplings](https://horizonffxi.wiki/Tree_Saplings) and [Ibwam](https://horizonffxi.wiki/Ibwam) separately reference `Babban Ny Mheillea` and `Lure of the Wildcat (Windurst)`; both remain `Unknown` fame because their individual quest pages and fame values are unresolved. `Trust: Windurst` has no resolved current Horizon source, so both its availability and fame remain `Unknown`. This produces 89 rows with some current Horizon evidence, four source-reported unavailable rows, one explicit availability unknown, and three unknown fame values.

Windurst uses a separate character-cache key while preserving the established Bastok and San d'Oria cache structures. Until its current and completed logs or a valid character cache exist, its rows remain `UNKNOWN`.

## Jeuno catalog

The [HorizonXI Jeuno Quests category](https://horizonffxi.wiki/Category:Jeuno_Quests), fetched on 2026-09-03, exposes 84 identifiable titles across its completed-log list and location tables. Eighty-three match the pinned XIchecklist Jeuno ordering. The custom `Omni Aketon` row has no matching client-log index and is not assigned an invented bit. The category's displayed total of 145 is not used to synthesize titles or indices: XIchecklist has 146 named client slots, of which 63 have no matching current category evidence and remain source `unknown`.

Seventy-nine imported entries have location-table rows: 32 numeric fame requirements and 47 explicitly unlisted values. Four additional entries appear only in the completed-log list (`Unlisted Qualities`, `Chameleon Capers`, `Regaining Trust`, and `Mixed Signals`), so their location and fame remain unresolved. Together with the 63 unmatched entries, that makes 67 unknown fame values. Case-only differences such as `Scattered Into Shadow`/`Scattered into Shadow` are matched conservatively. Source links retain the actual current category links, including red links; a category listing or expansion styling is not promoted to a claim of current implementation.

Jeuno is stored under its own `cached_state.jeuno_quests` key, with the same version-1 paired-log encoding. Existing nation/map keys and IDs are unchanged. Missing or invalid Jeuno cache data does not invalidate another area's cache. The existing checklist behavior is retained: an actual current/completed bit can establish a character state, but an unlisted source with neither bit set remains `UNKNOWN`, not `Not Accepted`.

The v0.13.0 zone audit also refreshed Selbina, Mhaura, Rabao, and Aht Urhgan Whitegate pages. Those zone rosters were not imported in that increment because they require different client quest-log groups and separate fame evidence. Its Whitegate source contained a Treasures of Aht Urhgan unavailable notice; that historical observation is not a blanket claim about current expansion availability.

## Other Areas catalog

Version 0.16.0 adds all 91 named Other-log slots from the same pinned XIchecklist `maps/story.lua`, through index 209. Commented placeholders and the commented Adoulin entries above index 1000 are excluded. The leading upstream `+` marker is removed from display titles without changing their indices. This is one client-log catalog, not independent Selbina/Mhaura packet groups.

The [HorizonXI Other Quests category](https://horizonffxi.wiki/Category:Other_Quests), fetched on 2026-09-03, has 60 location-table rows and 59 completed-list rows; all 60 unique titles map to the imported client slots. The table-only `The Big One` accounts for the difference. Its explicit cross mark is retained as `reported_inactive`. `An Understanding Overlord?`, `An Affable Adamantking?`, and `A Generous General?` show Verification Needed and therefore remain availability `unknown`, despite having source links and starting locations. The remaining 56 table entries are `wiki_listed`, not guaranteed current-server functionality. The 31 unmatched named client entries remain source unknowns. No title or index is invented from the category's displayed total.

The only non-case spelling alias is client `Trial-Size Trial by Lightning` to wiki `Trial Size Trial by Lightning`, at index 28. Actual table URLs, including red links, are retained. Category tables are the authority for imported NPC and location fields; zone-page summaries can differ and are not silently combined with them.

The source supplies 19 numeric fame values, 41 unlisted values, and no fame evidence for the 31 unmatched entries. The `Required Fame` column uses the source's Selbina/Mhaura labels for their 16 numeric requirements. The three Mog House requirements are 3, 5, and 7, but the table does not identify a nation or fame region, so they display `Fame X (see source)` with an explanatory tooltip. No conversion to another nation's fame is inferred. A dash means `Not listed`, not zero or no prerequisite.

Location views mirror the 13 table headings plus All Locations and Unresolved: 11 Selbina rows, 16 Mhaura, six Tavnazian Safehold Main Level, 13 Upper Level, three Mog House, four Oldton Movalpolos, and one each for La Theine Plateau, Altar Room, Monastic Cavern, Qulun Dome, Riverne - Site #A01, Carpenters' Landing, and Uleguerand Range.

`cached_state.other_quests` is a new version-1 paired-log cache; the existing keys and encoding are unchanged. Synthetic tests cover completed-first/partial receipt, index 209, clearing and loading separate character data, malformed caches, existing-area isolation, and the real catalog's unknown/unavailable boundaries. A live current/completed flag continues to establish character state under the existing rules; verification-needed entries with neither flag remain unknown.

## Outlands catalog

The [HorizonXI Outlands Quests category](https://horizonffxi.wiki/Category:Outlands_Quests), fetched on 2026-09-03, supplies 51 location-table rows matching the pinned XIchecklist ordering. All 57 named client slots (indices 1 through 203, with gaps) are retained. Five Voidwatch entries and Open Sesame have no matching category evidence and remain `unknown`; the separately checked [Open Sesame page](https://horizonffxi.wiki/Open_Sesame) has no text. Commented placeholders, including The Sahagin's Key at index 128, are excluded. The category's displayed total is not used to invent records. A wiki listing remains evidence only, not proof of current availability; this includes the table's fishing-rod upgrade rows.

The three Trial-Size Trial names match the wiki's Trial Size spelling. XIchecklist names both indices 163 and 164 Divine Might; the [LandSandBoat quest constants](https://github.com/LandSandBoat/server/blob/base/scripts/globals/quests.lua) independently identify `DIVINE_MIGHT = 163` and `DIVINE_MIGHT_REPEAT = 164`. This is an index cross-check only, not a Horizon implementation claim. The second row displays Divine Might (Repeat) and links to its corresponding wiki table target. Red-link quest rows open the real category evidence rather than an edit/create-page URL.

There are 23 numeric fame requirements, 28 unlisted values, and six unknowns. Numeric labels retain Kazham, Norg, or Rabao as used by the source table. [The Missing Piece](https://horizonffxi.wiki/The_Missing_Piece) is the one explicit override: its individual page requires Rabao (and Selbina) Fame 4, agreeing with Quests of Note, while the category's Rabao table leaves fame blank. Its tooltip documents that discrepancy. A dash does not mean zero fame or no prerequisite.

Locations contain 14 Kazham entries, 21 Norg, 11 Rabao, two The Shrine of Ru'Avitau, and one each Cape Teriggan, The Sanctuary of Zi'Tah, and Yuhtunga Jungle. Six unresolved entries remain in a separate view. Together with All Locations these form nine dropdown choices, without adding a top-level tab.

Local fetched references are retained outside the runtime package in the parent workspace's ignored `.firecrawl/` folder: `horizon-outlands-quests.md`, `horizon-outlands-missing-piece.md`, `horizon-outlands-open-sesame.md`, and `lsb-quests.md`. The last file's SHA-256 is `958585141a64068ca1e29ce074f2dfbdc747a2cc2c71091c897c77b9b0052f4b`. The shipped catalog holds the reviewed facts and links; runtime operation needs no web access.

Outlands uses passive `0x056` types `0x0078`/`0x00B8` and its own `cached_state.outlands_quests` version-1 pair. Existing settings, IDs, caches, packet shape, and state precedence are unchanged. Synthetic tests cover partial/current-first and completed-first receipt, separate Divine Might bits, high-index flags, malformed caches, character reset/restoration, other-area isolation, and source-unknown entries resolved by actual client flags. Deployment and live confirmation remain separate steps.

## Aht Urhgan catalog

The [HorizonXI Aht Urhgan Quests category](https://horizonffxi.wiki/Category:Aht_Urhgan_Quests), fetched on 2026-09-03, contains 72 matching location-table rows for all 72 named client slots in the pinned XIchecklist Aht Urhgan ordering. Indices range from 0 through 103 with gaps; placeholders and the separate Assault catalog are excluded. Location counts are Aht Urhgan Whitegate 53, Al Zahbi 3, Nashmau 9, Bastok Markets 1, Wajaom Woodlands 2, Arrapago Reef 3, and Mount Zhayolm 1. No Strings Attached remains in this client log despite starting in Bastok Markets. All Locations plus those seven headings make eight views.

Displayed spelling follows the wiki after exact case-insensitive matching. Explicit aliases resolve XIchecklist's simplified names at indices 15 (Cook-a-roon?), 18 (Totoroon's Treasure Hunt), 68/69 (the two VW Op. titles, including corrected Aht Urhgan/Subterranean spelling), 71 (Duties, Tasks, and Deeds), and 90-99 (the ten Promotion: titles). No fuzzy matching, shifted indices, or inferred missing rows are used. Red-link quests open the category evidence rather than an edit/create-page URL.

The category explicitly states that fame is not available in Aht Urhgan and omits the Fame column. Accordingly, all 72 rows show `N/A` with a source note, not a guessed Fame 0 or an assertion that there are no prerequisites. Jobs, levels, missions, mercenary rank, and other requirements are outside this column. All 72 availability labels are `wiki_listed`, including red-link and later-content listings; this is not proof of current implementation. The existing unknown-state and checkbox rules remain unchanged. Neither old zone-page availability prose nor the site's general expansion banner is used to classify individual quests as active or unavailable.

The [Windower packet definitions](https://github.com/Windower/Lua/blob/dev/addons/libs/packets/fields.lua) corroborate the pinned XIchecklist packet types: `0x0080` current and `0x00C0` completed. Both contain just 16 bytes of TOAU quest flags starting at `0x04`. The current packet then has Assault/TOAU/WOTG/Campaign mission integers at `0x14`-`0x20`; the completed packet has Assault flags from `0x14`. The decoder explicitly restricts Aht Urhgan to the first 128 bits and rejects higher indices. It never reads the remaining fields as quests.

The new `cached_state.ahturhgan_quests` version-1 pair stores 32 hexadecimal characters per half (16 bytes), rejects malformed or 32-byte area caches, and requires both current/completed halves. Existing area keys still use their unchanged 64-character halves. Synthetic tests cover both arrival orders, mission/Assault-tail changes that cannot affect the cache, boundary bits 127/128/255, real catalog statuses, truncated packets, malformed caches, character clear/restore, and existing-area isolation. Offline validation passed 34 Python tests, 14 Lua fixtures, and Lua 5.1 parsing for all 17 runtime files. In-game verification remains separate.

Local reference files are retained in the parent workspace's ignored `.firecrawl/` directory: `horizon-ahturhgan-quests.md` and `windower-quest-packet-fields.md`. The packet-definition snapshot SHA-256 is `75ec48a32985b05c0ce534e2a47aad161483a9f601f5418134dae44b996721aa`. Source facts are bundled in the runtime catalog; the addon makes no runtime network requests.

## Horizon custom quests and manual marks

The [HorizonXI Custom Content/Quests category](https://horizonffxi.wiki/Category:HorizonXI_Custom_Content/Quests), checked 2026-09-03, identifies four pages. All four are included under a separate Horizon Custom area with manually reported completion and no assigned client-log index. The [older Horizon Quests category](https://horizonffxi.wiki/Category:Horizon_Quests) additionally names Anniversary Scavenger Hunt 2023; that historical event is deferred, not silently treated as currently obtainable. This is an initial bounded catalog, not a claim that every custom quest is known.

- [Omni Aketon](https://horizonffxi.wiki/Omni_Aketon): Milia, Ru'Lude Gardens H-6; fame explicitly None. The page says this hidden quest does not appear in the quest log. Nation ranks and aketons are prerequisites, not completion signals. Reward: Ducal Aketon.
- [Dyer's Woad Quest](https://horizonffxi.wiki/Dyer%27s_Woad_Quest): Sorutoto, Windurst Woods G-12; fame explicitly None, no fame gain. Two Dyer's Woad yield 800 gil. Repeatable; the manual checkbox records completion at least once, not a repeat count.
- [Fill In](https://horizonffxi.wiki/Fill_In): Nanaa Mihgo, Windurst Woods J-3; Windurst fame is tagged Information Needed, so the value remains Unknown. Reward: Nanaa's Charm.
- [A Mind Unbound](https://horizonffxi.wiki/A_Mind_Unbound): Zonono at Halvung Staging Point, Mount Zhayolm K-6. The source labels the official title and quest-log description unconfirmed and tags its None fame field as Information Needed. Displayed name is provisional and fame remains Unknown. The source requires Puppetmaster as main job to begin and leaves additional prerequisites uncertain. Reward: Black Puppet Turban.

None of these names matched the pinned XIchecklist mapping. That establishes only that this addon has no confirmed reader; it does not prove that every custom quest lacks a client-log representation. Manual tracking was explicitly selected by the user. Four namespaced IDs in the existing character-scoped `manual_completed` table avoid legacy IDs and require no cache migration. The setter accepts booleans only and checks the canonical profile entry for explicit custom/manual tracking, no quest index, and wiki-listed evidence. Unknown IDs, mapped quests, spell/map rows, and legacy pilot IDs cannot be modified through this path. Repeated unchanged values do not save. Unchecking removes only that entry's own mark.

The states are distinctly named `manual_open` and `manual_complete`. Both count in known progress, which the UI scope note explicitly describes as including self-reported custom completion. Completed filtering applies to manual completion; unknown filtering continues to apply only to unknown completion/source state, not unknown fame. Mapped quest-state precedence, cached packets, and incoming-only behavior are unchanged.

References are retained in the parent workspace's ignored `.firecrawl/` folder: `horizon-custom-quests-index.md`, `horizon-exclusive-quests-index.md`, and the four `horizon-custom-omni-aketon.md`, `horizon-custom-dyers-woad.md`, `horizon-custom-fill-in.md`, and `horizon-custom-mind-unbound.md` pages. The shipped Lua data contains the reviewed facts and source links, without runtime web requests.

Synthetic tests cover the real addon action and settings callback using only fake character data: check/uncheck, no-op saves, reload restoration, two-character isolation, preservation of legacy marks and caches, rejection of mapped/unknown IDs, distinct states/totals, filters, and custom-only UI checkboxes. Offline validation passed 35 Python tests, 15 Lua fixtures, and Lua 5.1 parsing for all 18 runtime files. Real in-game persistence/UI validation remains pending.

## Quest details and Accepted-only view (2026-09-04)

The Accepted-only checkbox filters the existing snapshot to `auto_current` rows. It does not introduce a new tracking state, reinterpret completed repeatable quests, or create an Accepted state for manual custom quests. Both this filter and expanded-row IDs are session-only UI state. Catalog totals, packet readers, IDs, cache formats, and manual-mark persistence are unchanged.

The initial detail pass is limited to Bastok and the four Horizon Custom rows. Eighty-eight Bastok rows have sourced coordinates/reward references, and 81 have individual-page prerequisite summaries; seven table-only rows and five unresolved rows are not assigned invented prerequisites. Bastok coordinates and category reward summaries come from [the Bastok quest table](https://horizonffxi.wiki/Category:Bastok_Quests), saved as `.firecrawl/bastok-quest-details-table.md`. Individual quest pages are saved under `.firecrawl/quest-detail-<stable-id>.md`; each shipped row retains its quest Source URL. The developer-only `dev/quest_detail_table.py` reads these explicit local inputs and emits reviewed patches; the addon never fetches or imports web data itself.

Individual-page infobox rewards take precedence over category summaries except that obvious conflicting None/single-gil values are displayed as conflicting sources. [Gourmet](https://horizonffxi.wiki/Gourmet) preserves its 100-350 gil range rather than the category's maximum. [Ghosts of the Past](https://horizonffxi.wiki/Ghosts_of_the_Past) uses Tekko Kagi and excludes the crossed-out Beat Cesti reward. Source uncertainty markers remain unverified. Existing fame values are not silently changed: a differing numeric page requirement is flagged in the detail text.

Prerequisites are explicitly partial source summaries: fame, additional/level requirements, items needed, and labelled previous-quest links when listed in the infobox. Items can be needed to finish rather than start, and a previous-quest link is not inferred to be an automatically evaluated gate. No comprehensive walkthrough or live eligibility calculation is claimed. Missing fields say Not yet verified rather than None. Custom summaries use the four individually reviewed pages cited above, retaining Fill In and A Mind Unbound's uncertain fame/prerequisites.

The initial pass left other areas' missing fields explicit. Missions remain a separate future stage. All fetched material stays ignored and no character data is used in data-generation or test fixtures. A before/after semantic hash confirmed that removing the three newly added Bastok fields reproduces every pre-existing Bastok row exactly, including IDs, indices, fame, availability, descriptions, and ordering.

## Remaining-area detail pass (2026-09-04, foundation.18)

The same expandable view now has additional static reference fields for 393 existing quests across the remaining six areas. No quest rows, indices, NPC names/locations, fame-column values, availability labels, tracking logic, UI, or save formats are changed by this pass.

| Area | Existing quests | Coordinates | Reward references | Prerequisite summaries |
| --- | ---: | ---: | ---: | ---: |
| San d'Oria | 82 | 69 | 79 | 76 |
| Windurst | 90 | 84 | 87 | 85 |
| Jeuno | 146 | 79 | 78 | 72 |
| Other Areas | 91 | 56 | 60 | 55 |
| Outlands | 57 | 47 | 46 | 47 |
| Aht Urhgan | 72 | 41 | 41 | 40 |

Coordinates/category rewards use the previously captured category tables cited in each area section. Individual infoboxes were fetched on 2026-09-04 from the public [HorizonXI MediaWiki API](https://horizonffxi.wiki/w/api.php), restricted to existing Source targets. Raw JSON responses preserve wikitext line breaks, removed rewards, uncertainty markers, revision IDs, and timestamps; formatted API scrapes were not used for import because they lost that markup. Evidence is retained in this repository's ignored `.firecrawl/<area>-quest-raw-api-<batch>.json` files. The parent workspace's cached category files remain `search-horizonxi-{sandoria,windurst}-quests-fame.json` and `horizon-{jeuno,other,outlands,ahturhgan}-quests.md`.

The developer-only `dev/quest_wikitext.py` parses supported Quest Header/legacy Quest fields; `dev/quest_detail_table.py` joins exact Source titles, follows API-reported redirects, and emits narrow patches. Missing pages, unknown templates, and unsupported conditional markup are not flattened into facts. Where an infobox requirement could not be safely read, its summary explicitly says some requirements remain unverified. Existing category-only reward references retain that label. Closed strike-through rewards are excluded, optional/alternative rewards and timing qualifications retained, and conflicting numeric fame or obvious None/gil reward values called out. This is not an exhaustive conflict resolver or a statement of current server availability.

The [Borghertz Quest template](https://horizonffxi.wiki/Template:Borghertz_Quest), saved as `.firecrawl/borghertz-template-raw-api.json`, supplies the common artifact-hands wording: job 50+, the previous quest must have been started, no other hands quest active, and optional coffer rewards distinct from quest completion. The coffer-key alternative and corresponding-main-job restriction are retained. Other prerequisite summaries remain infobox-only, not a complete walkthrough or automatic eligibility check.

Synthetic parser tests cover nested templates/links, aliases, missing pages, legacy headers, rejected uncertainty/conditional markup, removed rewards, and source conflict handling. A runtime fixture pins coverage and representative conditional wording. Removing only the three added fields reproduces the exact pre-pass semantic hash for every row in each of the six catalogs. Offline validation passed 52 Python tests, 16 Lua fixture files, Lua 5.1 parsing for all 18 runtime files, and the whitespace check. Unverified gaps remain visible; Missions and live-server validation are still separate work.

## Bastok Missions (0.20.0 / foundation.19)

The [HorizonXI Bastok Missions category](https://horizonffxi.wiki/Category:Bastok_Missions), fetched 2026-09-04, lists 20 main missions, 1-1 through 9-2. Exact names map to the pinned [XIchecklist story catalog](https://github.com/HiPotionQ8/XIchecklist/blob/04baf17b2b0373883407a94ce0b6a72274a31ba6/maps/story.lua): IDs 0-5 and 10-23. The commented travel-stage names at IDs 6-9 identify parts of The Emissary; they are not four additional goals. New stable IDs are `bastok.mission.NNN`, separate from quests and legacy/custom manual marks. All 20 are wiki-listed, not certified live-server implementations. The profile now contains 1,043 entries; known/unknown totals still depend on available evidence and character logs.

Individual Mission Header infoboxes supply rewards and starting references. [Bastok Gate Guard](https://horizonffxi.wiki/Bastok_Gate_Guard) identifies Argus (Port Bastok L-7), Cleades (Bastok Markets D-11), Malduc (Metalworks J-8), and Rashid (Bastok Mines H-10). Magicite's transcluded [Nation Mission 4-1 walkthrough](https://horizonffxi.wiki/Nation_Mission_4-1) specifies Goggehn and the embassy back door at Ru'Lude Gardens H-10. All rows include coordinates and rewards; prerequisites remain partial references. Previous-mission links indicate sequence, not mandatory gates, and source level notes are not silently converted into entry requirements. Crystal quantities are not guessed from character rank-point state. Mission 3-2 remains explicitly optional; Magicite retains its alternate gil reward when the player already has an Airship Pass.

Research artifacts are ignored: `.firecrawl/bastok-missions.md`, `bastok-mission-1-1.md`, `bastok-missions-raw-api.json`, and `nation-mission-4-1-raw-api.json`. Raw API markup preserves conditional text and revision metadata; formatted page output is not used as raw wikitext. The 20-page raw response SHA-256 is `52873071770ac888a2eb72ed513eccf972f5987e1a4a7547fd73db2b268ba14b`. The developer-only `dev/bastok_mission_import.py` emits a reviewed catalog patch from these local inputs; there is no runtime fetch or automatic upstream import.

The cached [Windower packet definitions](https://github.com/Windower/Lua/blob/dev/addons/libs/packets/fields.lua) identify incoming `0x056` type `0x00D0`: eight Bastok completion bytes at zero-based offset `0x0C`; the other nations and Zilart are ignored. Type `0xFFFF` supplies a 32-bit nation at `0x04` and current nation mission at `0x08`, with type at `0x24`. Independent corroboration fetched 2026-09-04 comes from LandSandBoat's [mission packet](https://github.com/LandSandBoat/server/blob/base/src/map/packets/s2c/0x056_mission.cpp), [completion packet](https://github.com/LandSandBoat/server/blob/base/src/map/packets/s2c/0x056_mission_other.cpp), and [mission enums](https://github.com/LandSandBoat/server/blob/base/scripts/globals/missions.lua). These establish Bastok nation ID 1, The Emissary IDs 5-9, and NONE 65535. They corroborate a protocol mapping, not HorizonXI's live behavior. Local copies are `.firecrawl/lsb-mission.cpp`, `lsb-mission-other.cpp`, and `lsb-missions.lua`; the latter SHA-256 is `3259459761abf1dd79472aeedae84c8a9de43afa0064c8556b8f6f042fa413ce`.

The independent `mission_state.lua` reads only those two incoming packet variants and never requests packets. It rejects truncated payloads, invalid nations, malformed cache fields, and invalid mission indices. Current matching is gated by nation; the main Emissary row completes only from bit 5. An unexpected in-range current ID is not coerced to zero or used to infer completion. A current repeat counts complete once while remaining visible as Current / Done. Not current describes observed state, not availability or eligibility.

`cached_state.bastok_missions` is a new version-1 pair: 16 hex characters of completion flags, numeric nation, and current ID. Both packet halves must have arrived before a live pair is saved. Zoning clears partial live state and allows the saved pair as fallback; character settings callbacks clear/reload the mission reader independently of quest caches. Unknown root cache versions clear mission state as well. Existing IDs, data rows, packet readers, cache formats, and manual-mark rules are unchanged. No actual player settings were opened or edited during development.

Offline validation passed 52 Python tests, 18 Lua fixture files, parsing all 20 runtime files as Lua 5.1, and the whitespace check. New fixtures cover both packet orders, missing/invalid inputs, exact bit boundaries, unrelated quest/nation/expansion bytes, no rank/order inference, NONE and unexpected current IDs, nation changes, travel stages, current repeats, and real addon callbacks with synthetic two-character settings. UI fixtures cover one Missions tab, storyline/rank/current filters, Source alignment, expansion, narrow widths/scales, unchanged quest filters, and no navigation writes. In-game layout and packet observation remain pending; other storylines are deferred.

## Remaining Horizon-era Missions (0.21.0 / foundation.20)

The 2026-09-07 source pass adds the HorizonXI Wiki mission tables for [San d'Oria](https://horizonffxi.wiki/Category:San_d%27Oria_Missions), [Windurst](https://horizonffxi.wiki/Category:Windurst_Missions), [Rise of the Zilart](https://horizonffxi.wiki/Category:Rise_of_the_Zilart_Missions), [Chains of Promathia](https://horizonffxi.wiki/Category:Chains_of_Promathia_Missions), and [Treasures of Aht Urhgan](https://horizonffxi.wiki/Category:Treasures_of_Aht_Urhgan_Missions). The added catalogs contain 20, 20, 18, 34, and 48 rows respectively, for 160 mission rows and 1,183 profile entries overall. Names, types, rewards, ordering, and Source links come from the saved category pages. The new rows deliberately say `See Source` for NPC/location/coordinates rather than fabricating detail fields that were not present in the category tables. `dev/mission_catalog_import.py` reproduces the five generated catalogs from ignored `.firecrawl` inputs; the addon performs no runtime web access.

The Aht Urhgan category currently says it lists planned HorizonXI content. Its 48 rows are therefore reference-first and explicitly repeat that caveat; a wiki row is not treated as proof of current server availability. Wings of the Goddess, Assaults, add-on scenarios, Seekers of Adoulin, Rhapsodies of Vana'diel, and later storylines remain excluded from this scope.

The pinned [XIchecklist story catalog](https://github.com/HiPotionQ8/XIchecklist/blob/04baf17b2b0373883407a94ce0b6a72274a31ba6/maps/story.lua) supplies client mission IDs. San d'Oria and Windurst use main IDs 0-5 and 10-23; their travel stages 6-9 map only to Journey Abroad and The Three Kingdoms respectively. Zilart's sparse packet IDs and Promathia's mapped current values are preserved exactly. Promathia's long 3-3 and 5-3 branches accept only the explicitly listed XIchecklist sub-stage IDs; sequence ranges are not guessed.

The [Windower `0x056` definitions](https://github.com/Windower/Lua/blob/dev/addons/libs/packets/fields.lua) identify eight-byte completion slices for San d'Oria, Bastok, Windurst, and Zilart in type `0x00D0`, plus Aht Urhgan in type `0x00D8`. Current nation/Zilart/Promathia values come from `0xFFFF`; current Aht Urhgan comes from `0x0080`. LandSandBoat's [mission packet header](https://github.com/LandSandBoat/server/blob/base/src/map/packets/s2c/0x056_mission.h), [writer](https://github.com/LandSandBoat/server/blob/base/src/map/packets/s2c/0x056_mission.cpp), [completion writer](https://github.com/LandSandBoat/server/blob/base/src/map/packets/s2c/0x056_mission_other.cpp), and [mission enums](https://github.com/LandSandBoat/server/blob/base/scripts/globals/missions.lua) independently corroborate those layouts and identify the Zilart/Promathia declined-start bits. These sources establish protocol semantics, not live HorizonXI behavior.

Promathia has no reviewed completed-missions packet in this layout. Its exact current row can display `Current`, including reviewed multi-part IDs, but all other Promathia rows remain `UNKNOWN` and outside the progress denominator. The other five storylines require their current and completion halves before saving a per-character version-1 cache. Nation current state is gated by allegiance, explicit completion bits remain visible across allegiance changes, expansion-declined flags prevent mission-zero false positives, and unexpected current IDs remain unknown. Existing Bastok cache format and behavior are retained.

Offline validation passed 52 Python tests, 19 Lua fixture files, parsing all 25 runtime files as Lua 5.1, and the whitespace check. Synthetic fixtures cover every packet slice, both packet orders, completion-bit boundaries, nation gates, travel/branch aliases, expansion-declined flags, malformed caches, cache fallback, character isolation, and Promathia's current-only denominator behavior. In-game verification remains required for the five newly added storylines.

## Maintenance rule

New profile entries should have a stable ID, a source URL, an evidence label, and a concise description of any uncertainty. Do not infer `active`, `complete`, or `unavailable` from missing data. Changes that add packet parsing should document packet provenance and be tested with synthetic fixtures before any live-server review.
